{
  "name": "six-indicator",
  "version": "1.3.0",
  "packageManager": "yarn@4.5.3",
  "scripts": {
    "build": "web-ext build",
    "dev": "web-ext build --filename '{name}-{version}-snapshot.zip' --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
